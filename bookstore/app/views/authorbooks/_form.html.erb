<%= form_with(model: authorbook, local: true) do |form| %>
  <% if authorbook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(authorbook.errors.count, "error") %> prohibited this authorbook from being saved:</h2>

      <ul>
      <% authorbook.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :author_id %>
    <%= form.text_field :author_id %>
  </div>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.text_field :book_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
